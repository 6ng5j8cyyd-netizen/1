# 範例：Cyberpunk 2077（AppID 1091500）的每遊戲設定
# 複製到 ~/.steamplay/games.d/1091500.env 後，
# `steamplay launch 1091500` 會自動套用這些環境變數。

# 這款遊戲需要 AVX 指令集（macOS 15+ 的 Rosetta 2 才支援）
ROSETTA_ADVERTISE_AVX=1

# DX12 遊戲建議搭配 GPTK 引擎（D3DMetal）以獲得最佳效能
# 顯示 FPS 與 GPU 使用率以便調整畫質設定
MTL_HUD_ENABLED=1
